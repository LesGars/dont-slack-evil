service: dont-slack-evil

provider:
  name: aws
  runtime: go1.x
  profile: dont-slack-evil-hackaton

package:
  exclude:
    - ./**
  include:
    - ./bin/**

functions:
  hello:
    handler: bin/hello
    events:
      - http:
          path: hello
          method: get
